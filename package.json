{
  "name": "osmarpetry-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "rm -rf _site && eleventy --serve",
    "build": "rm -rf _site && eleventy",
    "build:prod": "rm -rf _site && NODE_ENV=production eleventy && node scripts/optimize.mjs",
    "optimize": "node scripts/optimize.mjs",
    "screenshots:capture": "node scripts/capture-screenshots.mjs",
    "covers:fill": "node scripts/fill-missing-covers.mjs",
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@storybook/html-vite": "^10.6.0",
    "html-minifier-terser": "^7.2.0",
    "lightningcss": "^1.32.0",
    "markdown-it": "^15.0.1",
    "markdown-it-anchor": "^9.2.1",
    "nunjucks": "^3.2.4",
    "playwright": "^1.63.0",
    "sharp": "^0.35.4",
    "storybook": "^10.3.4",
    "terser": "^5.51.2"
  }
}
